{
  "devDependencies": {
    "nodemon": "^3.1.7"
  },
  "scripts": {
    "start": "npm-run-all --parallel server client",
    "server": "cd server && npm start",
    "client": "cd client && npm run dev"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "npm-run-all": "^4.1.5",
    "uninstall": "^0.0.0"
  }
}
